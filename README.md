A pet notepad written in Ada using Gtk

Ada compiler and GtkAda bindings are required. Tested to compile and work under Linux.

Makefile is self-explanatory:

make compile -> creates ./noter

make run -> runs ./noter
