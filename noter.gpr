with "gtkada";

project Noter is
	for Main use ("noter.adb");
	for Source_Dirs use ("src");
	for Object_Dir use "obj";
	for Exec_Dir use ".";

	package Builder is
		for Executable ("noter.adb") use "noter";
	end Builder;

end Noter;